apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata:
  name: ledgerlens-ingestion-partition
  namespace: chaos-mesh
spec:
  action: partition
  mode: all
  selector:
    namespaces: ["ledgerlens-staging"]
    labelSelectors:
      "app.kubernetes.io/component": api
  direction: to
  target:
    selector:
      namespaces: ["ledgerlens-staging"]
      labelSelectors:
        "app.kubernetes.io/name": ingestion-worker
    mode: all
  duration: "60s"
